services:
  time-tracker:
    build: .
    container_name: time-tracker
    ports:
      - "8080:80"
    environment:
      - DB_PATH=/var/www/db/tracker.sqlite
    volumes:
      - .:/var/www/html:ro
      - tracker-data:/var/www/db
    restart: unless-stopped

volumes:
  tracker-data: