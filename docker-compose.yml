version: '3.8'

services:
  time-tracker:
    build: .
    container_name: time-tracker
    ports:
      - "8080:80"
    volumes:
      # Mount the current directory to the container.
      # This ensures the database file persists on your local machine.
      - .:/var/www/html
    restart: unless-stopped